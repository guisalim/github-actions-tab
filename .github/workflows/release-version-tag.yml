name: Bump version
on:
  push:
    branches:
      - v*

jobs:
  version-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          pre_release_branches: prerelease*
          create_annotated_tag: false
          fetch_all_tags: true
          custom_release_rules: minor:minor,patch:patch,major:major
          default_bump: false
      - name: Review Log
        run: echo "release type = ${{ steps.tag_version.outputs.release_type }}" 
      - name: Create a GitHub release
        if: steps.tag_version.outputs.release_type
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}
# 
# Steps to release new version:
# 
# [] Create new branch following the pattern: `release/v*`
# [] Create tag for the release version, example `v2.0.0`
# [] Commit to version branch using the 